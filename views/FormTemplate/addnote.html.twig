{% extends "layout.html.twig" %}

{% block title %}
    Formulaire d'ajout de note
{% endblock %}

{% block content %}

    <!-- form start -->
    <form class="form-horizontal" method="post" action="{{ path('note') }}">
        <div class="box-body" style="margin-left:2rem; width: 80%; ">
            <div class="form-group" style="width: 6%;  display:inline-block; margin:10px;">
                <label for="sel1">Note</label>
                <input type="text" class="form-control" name="note" />
            </div>
            <div class="form-group" style="width: 6%;  display:inline-block; margin:10px;">
                <label for="sel1">Etudiant</label>
                <select class="form-control" id="sel2" name="etudiant">
                    {% for etudiant in student %}
                    <option value={{ etudiant.id }}>{{ etudiant.student_name~' '~etudiant.student_firstname }}</option>
                    {% endfor %}
                </select>
            </div>
            {#<div class="form-group" style="width: 6%;  display:inline-block; margin:10px;">
                <label for="sel1">Classe</label>
                <select class="form-control" id="sel1">
                    {% for classe in classNames %}
                    <option>{{ classe.class_name }}</option>
                    {% endfor %}
                </select>
            </div>#}
            <div class="form-group" style="width: 6%;  display:inline-block; margin:10px;">
                <label for="sel1">Matière</label>
                <select class="form-control" id="sel3" name="matiere">
                    {% for matiere in matieres %}
                    <option>{{ matiere.name_discipline }}</option>
                    {% endfor %}
                </select>
            </div>
        </div>
          {# submit avec input si possible #}      
        <div class="form_group">
            <button type="submit" class="btn btn-info pull-right">Ajouter</button>
        </div>
        <!-- /.box-body -->
        <!-- mettre le submit ici -->
        <!-- /.box-footer -->
    </form>

    <!-- Main content -->
    <section class="content">
        <div class="row">
            <div class="col-xs-12">
                <div class="box">
                    <div class="box-header">
                        <h3 class="box-title">Autres</h3>
                    </div>
                    <!-- /.box-header -->
                    <div class="box-body">
       
                    </div>
                    <!-- /.box-body -->
                </div>
                <!-- /.box -->
            </div>
            <!-- /.col -->
        </div>
        <!-- /.row -->
    </section>
    <!-- /.content -->
    </div>
    <!-- /.content-wrapper -->


{% endblock %}

