{% extends "layout.html.twig" %}

{% block title %}
    Formulaire d'ajout de note
{% endblock %}

{% block content %}

    <!-- form start -->


    <!-- /.box-header -->
    <!-- form start -->
 {#   <form class="form-horizontal" method="post" action="{{ path('note') }}" style="margin-left:20px;">

        <div class="box-body" >
            <div class="form-group" style=" width: 110px; display:inline-block;">
                <label for="sel1">Note</label>
                <input  style="border-radius: 3px;" type="text" class="form-control" name="note" placeholder="Ex : 12/20"/>
            </div>

            <div class="form-group" style=" width:auto;  min-width:250px; max-width:500px; ">


                <label for="sel1">Etudiant</label>
                <select class="form-control" id="sel2" name="etudiant" style="border-radius: 3px;">
                    {% for etudiant in student %}
                        <option value="{{ etudiant.id_student }}">{{ etudiant.student_name~' '~etudiant.student_firstname }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="form-group" style=" width:auto;  min-width:250px; max-width:500px;">
                <label for="sel1">Matière</label>
                <select class="form-control" id="sel3" name="matiere"  style="border-radius: 3px;">
                    {% for matiere in matieres %}
                        <option value="{{ matiere.id_discipline }}">{{ matiere.name_discipline }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="form_group">
                <button type="submit" class="btn btn-info pull-right">save</button>
            </div>
        </div>
   

        <!-- /.box-body -->
        <!-- mettre le submit ici -->
        <!-- /.box-footer -->
{#    </form>

    </div>


    <!-- Main content -->
    <section class="content">
        <div class="row">
            <div class="col-xs-12">
                <div class="box">
                    <div class="box-header">
                        <h3 class="box-title">Observations</h3>
                    </div>
                    <!-- /.box-header -->
                    <div class="box-body">
     
                        
                        
                        
        <form class="form-horizontal" method="post" action="{{ path('judgement') }}">

           <div class="box-body" style="box-shadow: 3px 2px #333">

               <textarea  style="border-radius: 3px; width:100%; margin:3px" name="judgement"></textarea>

               <div class="form_group">
                   <button type="submit" class="btn btn-info pull-right" style="float:right; margin:5px;">Ajouter</button>
               </div>
        
            </div>   
                        
        </form>
            #}

<div class="box box-primary">   
    <!-- Main content -->
    <form class="form-horizontal" method="post" action="{{ path('note') }}" style="margin-left:20px">

              <div class="box-body">
                    <div class="form-group">
                      <label for="#">Note</label>
                      <input style="border-radius: 3px; width: 15%" type="text" class="form-control" name="note" placeholder="Ex : 13"/>
                        <label for="#">Coefficient</label>
                        <input style="border-radius: 3px; width: 15%" type="text" class="form-control" name="coeff" placeholder="Ex : 1"/>
                    </div>

                    <div class="form-group">
                      <label for="#">Etudiant</label>
                      <select class="form-control" id="sel2" name="etudiant" style="border-radius: 3px; width: 30%">
                          {% for etudiant in student %}
                              <option value="{{ etudiant.id_student }}">{{ etudiant.student_name~' '~etudiant.student_firstname }}</option>
                          {% endfor %}
                      </select>
                    </div>

                    <div class="form-group">
                      <label for="#">Matière</label>
                      <select class="form-control" id="sel3" name="matiere"  style="border-radius: 3px; width: 31%">
                          {% for matiere in matieres %}
                              <option value="{{ matiere.id_discipline }}">{{ matiere.name_discipline }}</option>
                          {% endfor %}
                      </select>
                    </div>

                    <div class="form-group">
                        <label for="#">Observations</label>
                        <textarea  class="form-control" rows="3" style="width: 30%; border-radius: 3px" name="judgement" placeholder="Ex : Bon travail, travaillez mieux la méthode vue en cours ..."></textarea>
                    </div>
                    <div class="form_group">
                      <button type="submit" class="btn btn-info pull-right">Ajouter</button>
                    </div>
              </div>
    </form>
        <!-- /.row -->
</div>
    <!-- /.content-wrapper -->



{% endblock %}

